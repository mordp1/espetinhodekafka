# Kafka Producer Configuration
# Use this file with: PRODUCER_CONFIG=producer.config ./benchmark-runner.sh

# ===========================
# Basic Settings
# ===========================
bootstrap.servers=localhost:29092,localhost:39092,localhost:49092
acks=1

# ===========================
# TLS/SSL Configuration (uncomment if needed)
# ===========================
# For TLS-enabled Kafka clusters, uncomment and configure:
#security.protocol=SSL
#ssl.truststore.location=/path/to/kafka.client.truststore.jks
#ssl.truststore.password=your-truststore-password
#ssl.keystore.location=/path/to/kafka.client.keystore.jks
#ssl.keystore.password=your-keystore-password
#ssl.key.password=your-key-password

# For two-way SSL (mutual TLS), also set:
#ssl.endpoint.identification.algorithm=

# ===========================
# SASL/PLAIN Configuration (uncomment if needed)
# ===========================
#security.protocol=SASL_SSL
#sasl.mechanism=PLAIN
#sasl.jaas.config=org.apache.kafka.common.security.plain.PlainLoginModule required \
#  username="your-username" \
#  password="your-password";

# ===========================
# SASL/SCRAM Configuration (uncomment if needed)
# ===========================
#security.protocol=SASL_SSL
#sasl.mechanism=SCRAM-SHA-256
#sasl.jaas.config=org.apache.kafka.common.security.scram.ScramLoginModule required \
#  username="your-username" \
#  password="your-password";

# ===========================
# AWS MSK IAM Authentication (uncomment if needed)
# ===========================
#security.protocol=SASL_SSL
#sasl.mechanism=AWS_MSK_IAM
#sasl.jaas.config=software.amazon.msk.auth.iam.IAMLoginModule required;
#sasl.client.callback.handler.class=software.amazon.msk.auth.iam.IAMClientCallbackHandler

# ===========================
# Performance Tuning
# ===========================

# Batching - larger batches improve throughput but increase latency
batch.size=16384
linger.ms=0

# Compression - reduces network usage, increases CPU
# Options: none, gzip, snappy, lz4, zstd
#compression.type=lz4

# Buffer memory - total memory for buffering
buffer.memory=33554432

# Max blocking time for buffer full or metadata unavailable
max.block.ms=60000

# In-flight requests - higher values improve throughput but may reorder messages
max.in.flight.requests.per.connection=5

# Request timeout
request.timeout.ms=30000

# ===========================
# Reliability Settings
# ===========================

# Acknowledgments - options: 0 (none), 1 (leader only), all (all replicas)
# acks=0    # Fastest, no guarantee
# acks=1    # Balanced (default above)
# acks=all  # Slowest, most reliable

# Enable idempotence for exactly-once semantics (requires acks=all)
#enable.idempotence=true
#max.in.flight.requests.per.connection=5
#acks=all

# Retries
retries=2147483647
max.in.flight.requests.per.connection=5

# ===========================
# Common Scenarios
# ===========================

# HIGH THROUGHPUT CONFIG:
# batch.size=65536
# linger.ms=10
# compression.type=lz4
# acks=1

# LOW LATENCY CONFIG:
# batch.size=0
# linger.ms=0
# compression.type=none
# acks=1

# HIGH DURABILITY CONFIG:
# acks=all
# enable.idempotence=true
# max.in.flight.requests.per.connection=5
# min.insync.replicas=2 (broker config, not here)